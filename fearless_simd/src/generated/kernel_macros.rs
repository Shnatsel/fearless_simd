// Copyright 2025 the Fearless_SIMD Authors
// SPDX-License-Identifier: Apache-2.0 OR MIT

// This file is autogenerated by fearless_simd_gen

#[doc = "Defines a safe kernel for `Neon`."]
#[doc = ""]
#[doc = "Generic kernels only accept type parameters with no bound or one path-like bound, such as `<S: Simd, T: Copy>`."]
#[doc = ""]
#[doc = "Kernel macros only accept safe functions."]
#[doc = ""]
#[doc = "```compile_fail"]
#[doc = "fearless_simd::neon_kernel! {"]
#[doc = "    unsafe fn should_not_compile() {}"]
#[doc = "}"]
#[doc = "```"]
#[doc = ""]
#[doc = "```compile_fail"]
#[doc = "fearless_simd::neon_kernel! {"]
#[doc = "    fn should_not_compile<T: Copy + Clone>(x: T) -> T { x }"]
#[doc = "}"]
#[doc = "```"]
#[macro_export]
macro_rules! neon_kernel {
    (
        $(#[$meta:meta])*
        $vis:vis fn $name:ident <
            $($generic:ident $(: $generic_bound:path)?),+ $(,)?
        >(
            $($arg:ident : $arg_ty:ty),* $(,)?
        ) $(-> $ret:ty)? {
            $($kernel_body:tt)*
        }
    ) => {
        #[cfg(target_arch = "aarch64")]
        $(#[$meta])*
        $vis fn $name<
            $($generic $(: $generic_bound)?),+
        >(
            _simd: $crate::Neon,
            $($arg: $arg_ty),*
        ) $(-> $ret)? {
            #[inline]
            #[target_feature(enable = "neon")]
            fn __fearless_simd_kernel<
                $($generic $(: $generic_bound)?),+
            >(
                $($arg: $arg_ty),*
            ) $(-> $ret)? {
                $($kernel_body)*
            }

            // SAFETY: the `Neon` token proves that the required target features are available.
            unsafe { __fearless_simd_kernel::<$($generic),+>($($arg),*) }
        }
    };

    (
        $(#[$meta:meta])*
        $vis:vis fn $name:ident(
            $($arg:ident : $arg_ty:ty),* $(,)?
        ) $(-> $ret:ty)? {
            $($kernel_body:tt)*
        }
    ) => {
        #[cfg(target_arch = "aarch64")]
        $(#[$meta])*
        $vis fn $name(
            _simd: $crate::Neon,
            $($arg: $arg_ty),*
        ) $(-> $ret)? {
            #[inline]
            #[target_feature(enable = "neon")]
            fn __fearless_simd_kernel(
                $($arg: $arg_ty),*
            ) $(-> $ret)? {
                $($kernel_body)*
            }

            // SAFETY: the `Neon` token proves that the required target features are available.
            unsafe { __fearless_simd_kernel($($arg),*) }
        }
    };

    ($($unsupported:tt)*) => {
        compile_error!(
            "kernel macros support only safe functions with non-generic signatures or simple type generics like `<S: Simd, T: Copy>`; use a wrapper function for lifetimes, const generics, `where` clauses, or multiple bounds"
        );
    };
}

#[doc = "Defines a safe kernel for `WasmSimd128`."]
#[doc = ""]
#[doc = "Generic kernels only accept type parameters with no bound or one path-like bound, such as `<S: Simd, T: Copy>`."]
#[doc = ""]
#[doc = "Kernel macros only accept safe functions."]
#[doc = ""]
#[doc = "```compile_fail"]
#[doc = "fearless_simd::wasm_simd128_kernel! {"]
#[doc = "    unsafe fn should_not_compile() {}"]
#[doc = "}"]
#[doc = "```"]
#[doc = ""]
#[doc = "```compile_fail"]
#[doc = "fearless_simd::wasm_simd128_kernel! {"]
#[doc = "    fn should_not_compile<T: Copy + Clone>(x: T) -> T { x }"]
#[doc = "}"]
#[doc = "```"]
#[macro_export]
macro_rules! wasm_simd128_kernel {
    (
        $(#[$meta:meta])*
        $vis:vis fn $name:ident <
            $($generic:ident $(: $generic_bound:path)?),+ $(,)?
        >(
            $($arg:ident : $arg_ty:ty),* $(,)?
        ) $(-> $ret:ty)? {
            $($kernel_body:tt)*
        }
    ) => {
        #[cfg(all(target_arch = "wasm32", target_feature = "simd128"))]
        $(#[$meta])*
        $vis fn $name<
            $($generic $(: $generic_bound)?),+
        >(
            _simd: $crate::WasmSimd128,
            $($arg: $arg_ty),*
        ) $(-> $ret)? {
            #[inline]
            fn __fearless_simd_kernel<
                $($generic $(: $generic_bound)?),+
            >(
                $($arg: $arg_ty),*
            ) $(-> $ret)? {
                $($kernel_body)*
            }

            let _ = _simd;
            __fearless_simd_kernel::<$($generic),+>($($arg),*)
        }
    };

    (
        $(#[$meta:meta])*
        $vis:vis fn $name:ident(
            $($arg:ident : $arg_ty:ty),* $(,)?
        ) $(-> $ret:ty)? {
            $($kernel_body:tt)*
        }
    ) => {
        #[cfg(all(target_arch = "wasm32", target_feature = "simd128"))]
        $(#[$meta])*
        $vis fn $name(
            _simd: $crate::WasmSimd128,
            $($arg: $arg_ty),*
        ) $(-> $ret)? {
            #[inline]
            fn __fearless_simd_kernel(
                $($arg: $arg_ty),*
            ) $(-> $ret)? {
                $($kernel_body)*
            }

            let _ = _simd;
            __fearless_simd_kernel($($arg),*)
        }
    };

    ($($unsupported:tt)*) => {
        compile_error!(
            "kernel macros support only safe functions with non-generic signatures or simple type generics like `<S: Simd, T: Copy>`; use a wrapper function for lifetimes, const generics, `where` clauses, or multiple bounds"
        );
    };
}

#[doc = "Defines a safe kernel for `Sse4_2`."]
#[doc = ""]
#[doc = "Generic kernels only accept type parameters with no bound or one path-like bound, such as `<S: Simd, T: Copy>`."]
#[doc = ""]
#[doc = "Kernel macros only accept safe functions."]
#[doc = ""]
#[doc = "```compile_fail"]
#[doc = "fearless_simd::sse4_2_kernel! {"]
#[doc = "    unsafe fn should_not_compile() {}"]
#[doc = "}"]
#[doc = "```"]
#[doc = ""]
#[doc = "```compile_fail"]
#[doc = "fearless_simd::sse4_2_kernel! {"]
#[doc = "    fn should_not_compile<T: Copy + Clone>(x: T) -> T { x }"]
#[doc = "}"]
#[doc = "```"]
#[macro_export]
macro_rules! sse4_2_kernel {
    (
        $(#[$meta:meta])*
        $vis:vis fn $name:ident <
            $($generic:ident $(: $generic_bound:path)?),+ $(,)?
        >(
            $($arg:ident : $arg_ty:ty),* $(,)?
        ) $(-> $ret:ty)? {
            $($kernel_body:tt)*
        }
    ) => {
        #[cfg(any(target_arch = "x86", target_arch = "x86_64"))]
        $(#[$meta])*
        $vis fn $name<
            $($generic $(: $generic_bound)?),+
        >(
            _simd: $crate::Sse4_2,
            $($arg: $arg_ty),*
        ) $(-> $ret)? {
            #[inline]
            #[target_feature(enable = "sse4.2,cmpxchg16b,popcnt")]
            fn __fearless_simd_kernel<
                $($generic $(: $generic_bound)?),+
            >(
                $($arg: $arg_ty),*
            ) $(-> $ret)? {
                $($kernel_body)*
            }

            // SAFETY: the `Sse4_2` token proves that the required target features are available.
            unsafe { __fearless_simd_kernel::<$($generic),+>($($arg),*) }
        }
    };

    (
        $(#[$meta:meta])*
        $vis:vis fn $name:ident(
            $($arg:ident : $arg_ty:ty),* $(,)?
        ) $(-> $ret:ty)? {
            $($kernel_body:tt)*
        }
    ) => {
        #[cfg(any(target_arch = "x86", target_arch = "x86_64"))]
        $(#[$meta])*
        $vis fn $name(
            _simd: $crate::Sse4_2,
            $($arg: $arg_ty),*
        ) $(-> $ret)? {
            #[inline]
            #[target_feature(enable = "sse4.2,cmpxchg16b,popcnt")]
            fn __fearless_simd_kernel(
                $($arg: $arg_ty),*
            ) $(-> $ret)? {
                $($kernel_body)*
            }

            // SAFETY: the `Sse4_2` token proves that the required target features are available.
            unsafe { __fearless_simd_kernel($($arg),*) }
        }
    };

    ($($unsupported:tt)*) => {
        compile_error!(
            "kernel macros support only safe functions with non-generic signatures or simple type generics like `<S: Simd, T: Copy>`; use a wrapper function for lifetimes, const generics, `where` clauses, or multiple bounds"
        );
    };
}

#[doc = "Defines a safe kernel for `Avx2`."]
#[doc = ""]
#[doc = "Generic kernels only accept type parameters with no bound or one path-like bound, such as `<S: Simd, T: Copy>`."]
#[doc = ""]
#[doc = "Kernel macros only accept safe functions."]
#[doc = ""]
#[doc = "```compile_fail"]
#[doc = "fearless_simd::avx2_kernel! {"]
#[doc = "    unsafe fn should_not_compile() {}"]
#[doc = "}"]
#[doc = "```"]
#[doc = ""]
#[doc = "```compile_fail"]
#[doc = "fearless_simd::avx2_kernel! {"]
#[doc = "    fn should_not_compile<T: Copy + Clone>(x: T) -> T { x }"]
#[doc = "}"]
#[doc = "```"]
#[macro_export]
macro_rules! avx2_kernel {
    (
        $(#[$meta:meta])*
        $vis:vis fn $name:ident <
            $($generic:ident $(: $generic_bound:path)?),+ $(,)?
        >(
            $($arg:ident : $arg_ty:ty),* $(,)?
        ) $(-> $ret:ty)? {
            $($kernel_body:tt)*
        }
    ) => {
        #[cfg(any(target_arch = "x86", target_arch = "x86_64"))]
        $(#[$meta])*
        $vis fn $name<
            $($generic $(: $generic_bound)?),+
        >(
            _simd: $crate::Avx2,
            $($arg: $arg_ty),*
        ) $(-> $ret)? {
            #[inline]
            #[target_feature(enable = "avx2,bmi1,bmi2,cmpxchg16b,f16c,fma,lzcnt,movbe,popcnt,xsave")]
            fn __fearless_simd_kernel<
                $($generic $(: $generic_bound)?),+
            >(
                $($arg: $arg_ty),*
            ) $(-> $ret)? {
                $($kernel_body)*
            }

            // SAFETY: the `Avx2` token proves that the required target features are available.
            unsafe { __fearless_simd_kernel::<$($generic),+>($($arg),*) }
        }
    };

    (
        $(#[$meta:meta])*
        $vis:vis fn $name:ident(
            $($arg:ident : $arg_ty:ty),* $(,)?
        ) $(-> $ret:ty)? {
            $($kernel_body:tt)*
        }
    ) => {
        #[cfg(any(target_arch = "x86", target_arch = "x86_64"))]
        $(#[$meta])*
        $vis fn $name(
            _simd: $crate::Avx2,
            $($arg: $arg_ty),*
        ) $(-> $ret)? {
            #[inline]
            #[target_feature(enable = "avx2,bmi1,bmi2,cmpxchg16b,f16c,fma,lzcnt,movbe,popcnt,xsave")]
            fn __fearless_simd_kernel(
                $($arg: $arg_ty),*
            ) $(-> $ret)? {
                $($kernel_body)*
            }

            // SAFETY: the `Avx2` token proves that the required target features are available.
            unsafe { __fearless_simd_kernel($($arg),*) }
        }
    };

    ($($unsupported:tt)*) => {
        compile_error!(
            "kernel macros support only safe functions with non-generic signatures or simple type generics like `<S: Simd, T: Copy>`; use a wrapper function for lifetimes, const generics, `where` clauses, or multiple bounds"
        );
    };
}
